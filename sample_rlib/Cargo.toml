[package]
name = "shadow-rs"
version = "2.2.0"
edition = "2021"
publish = false

[workspace]

[lib]
# the rlib crate is so that we build and run doctests
crate-type = ["rlib", "staticlib"]
path = "lib.rs"
