[package]
name = "sample"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
hashbrown = "0.12.0"
rand_core = "0.6"

[features]
default = ["superbanana"]
superbanana = []

[profile.release]
debug = "line-tables-only"
